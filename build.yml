---

# An array of names of players found in source/players
players: [flv, html, iframe, img, qt, swf]

# YES to compress the code, NO to use the source code
compress: NO

# The directory to output the build to
target: build
